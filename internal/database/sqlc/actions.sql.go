// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: actions.sql

package sqlc

import (
	"context"
)

const getActionTimingMetrics = `-- name: GetActionTimingMetrics :one
WITH action_durations AS (
    SELECT
        cache_hit,
        CASE
            WHEN start_time IS NOT NULL
                AND end_time IS NOT NULL
                AND end_time >= start_time
            THEN EXTRACT(EPOCH FROM (end_time - start_time)) * 1000
        END AS duration_in_ms
    FROM actions
    WHERE bazel_invocation_id = $1
)
SELECT
    COALESCE(FLOOR(SUM(duration_in_ms)), 0)::bigint AS total_expected_time_in_ms,
    COALESCE(
        FLOOR(SUM(duration_in_ms) FILTER (WHERE cache_hit IS TRUE)),
        0
    )::bigint AS time_saved_by_cache_hits_in_ms,
    COUNT(*)::bigint AS total_actions,
    COUNT(duration_in_ms)::bigint AS timed_actions,
    COUNT(*) FILTER (WHERE cache_hit IS TRUE)::bigint AS cache_hit_actions,
    COUNT(duration_in_ms) FILTER (WHERE cache_hit IS TRUE)::bigint AS timed_cache_hit_actions
FROM action_durations
`

type GetActionTimingMetricsRow struct {
	TotalExpectedTimeInMs    int64
	TimeSavedByCacheHitsInMs int64
	TotalActions             int64
	TimedActions             int64
	CacheHitActions          int64
	TimedCacheHitActions     int64
}

func (q *Queries) GetActionTimingMetrics(ctx context.Context, bazelInvocationID int64) (GetActionTimingMetricsRow, error) {
	row := q.db.QueryRowContext(ctx, getActionTimingMetrics, bazelInvocationID)
	var i GetActionTimingMetricsRow
	err := row.Scan(
		&i.TotalExpectedTimeInMs,
		&i.TimeSavedByCacheHitsInMs,
		&i.TotalActions,
		&i.TimedActions,
		&i.CacheHitActions,
		&i.TimedCacheHitActions,
	)
	return i, err
}
