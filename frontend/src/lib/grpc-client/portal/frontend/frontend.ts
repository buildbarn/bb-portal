// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.19.1
// source: portal/frontend/frontend.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Empty } from "../../google/protobuf/empty";

export const protobufPackage = "buildbarn.configuration.frontend";

/**
 * Configuration for the portal frontend. This configuration is injected into
 * the frontend by the backend when the index.html is served.
 */
export interface PortalFrontendConfiguration {
  /**
   * Feature flags used to turn features in the frontend on/off, such as
   * certain pages or sections of pages. Make sure that the feature flags are
   * in sync with the services configured for the backend.
   *
   * The frontend feature flags only affect the frontend. The underlying APIs
   * are still available, so just using these flags is not enough to secure
   * your confidential data.
   */
  featureFlags:
    | PortalFrontendConfiguration_FeatureFlags
    | undefined;
  /**
   * The URL of the BES gRPC backend. Used to tell users where to point their
   * `--bes_backend` flag to.
   */
  grpcBackendUrl: string;
  /** Used to personalize the title of the frontend. */
  companyName: string;
  /**
   * Customize the content of the footer. These elements are shown in a row at
   * the bottom of the page, and can be used to link out to useful places.
   */
  footerContent: PortalFrontendConfiguration_FooterElement[];
}

export interface PortalFrontendConfiguration_FeatureFlags {
  /** Enable/Disable features on the starting page of the interface. */
  home:
    | PortalFrontendConfiguration_FeatureFlags_HomePage
    | undefined;
  /** Enable/Disable the Build Event Stream interface, or parts of it. */
  bes:
    | PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags
    | undefined;
  /**
   * Enable/Disable the browser tab and pages. The browser pages is used to
   * browse the contents of the CAS and Action Cache. Disable if you are
   * using bb-browser.
   */
  browser:
    | Empty
    | undefined;
  /**
   * Enable/Disable the scheduler and operations tabs and pages. Disable this
   * if you don't want to connect Portal to the scheduler of a Buildbarn
   * cluster.
   */
  scheduler: Empty | undefined;
}

export interface PortalFrontendConfiguration_FeatureFlags_HomePage {
  /** Enables/Disables the BEP file upload area on the home page. */
  fileUpload:
    | Empty
    | undefined;
  /** Enables/Disables the BES upload instructions on the home page. */
  instructions: Empty | undefined;
}

export interface PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags {
  /** Enables/Disables the builds page and sub-pages. */
  pageBuilds:
    | Empty
    | undefined;
  /** Enables/Disables the invocations page and sub-pages. */
  pageInvocations:
    | Empty
    | undefined;
  /**
   * Enables/Disables the targets page and sub-pages, as well as the
   * targets tab on a invocation.
   */
  pageTargets:
    | Empty
    | undefined;
  /**
   * Enables/Disables the tests page and sub-pages, as well as the tests
   * tab on a invocation.
   */
  pageTests:
    | Empty
    | undefined;
  /** Enables/Disables the trends page. */
  pageTrends: Empty | undefined;
}

export interface PortalFrontendConfiguration_FooterElement {
  /** The text to display in the footer. */
  text: string;
  /** The URL to link to. Optional. */
  href: string;
  /** Icon for the footer element. Optional. */
  icon: PortalFrontendConfiguration_FooterElement_Icon | undefined;
}

export interface PortalFrontendConfiguration_FooterElement_Icon {
  url?: string | undefined;
  slack?: Empty | undefined;
  github?: Empty | undefined;
  discord?: Empty | undefined;
}

function createBasePortalFrontendConfiguration(): PortalFrontendConfiguration {
  return { featureFlags: undefined, grpcBackendUrl: "", companyName: "", footerContent: [] };
}

export const PortalFrontendConfiguration: MessageFns<PortalFrontendConfiguration> = {
  encode(message: PortalFrontendConfiguration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.featureFlags !== undefined) {
      PortalFrontendConfiguration_FeatureFlags.encode(message.featureFlags, writer.uint32(10).fork()).join();
    }
    if (message.grpcBackendUrl !== "") {
      writer.uint32(18).string(message.grpcBackendUrl);
    }
    if (message.companyName !== "") {
      writer.uint32(26).string(message.companyName);
    }
    for (const v of message.footerContent) {
      PortalFrontendConfiguration_FooterElement.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortalFrontendConfiguration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortalFrontendConfiguration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.featureFlags = PortalFrontendConfiguration_FeatureFlags.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.grpcBackendUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.companyName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.footerContent.push(PortalFrontendConfiguration_FooterElement.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortalFrontendConfiguration {
    return {
      featureFlags: isSet(object.featureFlags)
        ? PortalFrontendConfiguration_FeatureFlags.fromJSON(object.featureFlags)
        : isSet(object.feature_flags)
        ? PortalFrontendConfiguration_FeatureFlags.fromJSON(object.feature_flags)
        : undefined,
      grpcBackendUrl: isSet(object.grpcBackendUrl)
        ? globalThis.String(object.grpcBackendUrl)
        : isSet(object.grpc_backend_url)
        ? globalThis.String(object.grpc_backend_url)
        : "",
      companyName: isSet(object.companyName)
        ? globalThis.String(object.companyName)
        : isSet(object.company_name)
        ? globalThis.String(object.company_name)
        : "",
      footerContent: globalThis.Array.isArray(object?.footerContent)
        ? object.footerContent.map((e: any) => PortalFrontendConfiguration_FooterElement.fromJSON(e))
        : globalThis.Array.isArray(object?.footer_content)
        ? object.footer_content.map((e: any) => PortalFrontendConfiguration_FooterElement.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PortalFrontendConfiguration): unknown {
    const obj: any = {};
    if (message.featureFlags !== undefined) {
      obj.featureFlags = PortalFrontendConfiguration_FeatureFlags.toJSON(message.featureFlags);
    }
    if (message.grpcBackendUrl !== "") {
      obj.grpcBackendUrl = message.grpcBackendUrl;
    }
    if (message.companyName !== "") {
      obj.companyName = message.companyName;
    }
    if (message.footerContent?.length) {
      obj.footerContent = message.footerContent.map((e) => PortalFrontendConfiguration_FooterElement.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PortalFrontendConfiguration>): PortalFrontendConfiguration {
    return PortalFrontendConfiguration.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PortalFrontendConfiguration>): PortalFrontendConfiguration {
    const message = createBasePortalFrontendConfiguration();
    message.featureFlags = (object.featureFlags !== undefined && object.featureFlags !== null)
      ? PortalFrontendConfiguration_FeatureFlags.fromPartial(object.featureFlags)
      : undefined;
    message.grpcBackendUrl = object.grpcBackendUrl ?? "";
    message.companyName = object.companyName ?? "";
    message.footerContent =
      object.footerContent?.map((e) => PortalFrontendConfiguration_FooterElement.fromPartial(e)) || [];
    return message;
  },
};

function createBasePortalFrontendConfiguration_FeatureFlags(): PortalFrontendConfiguration_FeatureFlags {
  return { home: undefined, bes: undefined, browser: undefined, scheduler: undefined };
}

export const PortalFrontendConfiguration_FeatureFlags: MessageFns<PortalFrontendConfiguration_FeatureFlags> = {
  encode(message: PortalFrontendConfiguration_FeatureFlags, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.home !== undefined) {
      PortalFrontendConfiguration_FeatureFlags_HomePage.encode(message.home, writer.uint32(10).fork()).join();
    }
    if (message.bes !== undefined) {
      PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags.encode(message.bes, writer.uint32(18).fork()).join();
    }
    if (message.browser !== undefined) {
      Empty.encode(message.browser, writer.uint32(26).fork()).join();
    }
    if (message.scheduler !== undefined) {
      Empty.encode(message.scheduler, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortalFrontendConfiguration_FeatureFlags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortalFrontendConfiguration_FeatureFlags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.home = PortalFrontendConfiguration_FeatureFlags_HomePage.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bes = PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.browser = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.scheduler = Empty.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortalFrontendConfiguration_FeatureFlags {
    return {
      home: isSet(object.home) ? PortalFrontendConfiguration_FeatureFlags_HomePage.fromJSON(object.home) : undefined,
      bes: isSet(object.bes)
        ? PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags.fromJSON(object.bes)
        : undefined,
      browser: isSet(object.browser) ? Empty.fromJSON(object.browser) : undefined,
      scheduler: isSet(object.scheduler) ? Empty.fromJSON(object.scheduler) : undefined,
    };
  },

  toJSON(message: PortalFrontendConfiguration_FeatureFlags): unknown {
    const obj: any = {};
    if (message.home !== undefined) {
      obj.home = PortalFrontendConfiguration_FeatureFlags_HomePage.toJSON(message.home);
    }
    if (message.bes !== undefined) {
      obj.bes = PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags.toJSON(message.bes);
    }
    if (message.browser !== undefined) {
      obj.browser = Empty.toJSON(message.browser);
    }
    if (message.scheduler !== undefined) {
      obj.scheduler = Empty.toJSON(message.scheduler);
    }
    return obj;
  },

  create(base?: DeepPartial<PortalFrontendConfiguration_FeatureFlags>): PortalFrontendConfiguration_FeatureFlags {
    return PortalFrontendConfiguration_FeatureFlags.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PortalFrontendConfiguration_FeatureFlags>): PortalFrontendConfiguration_FeatureFlags {
    const message = createBasePortalFrontendConfiguration_FeatureFlags();
    message.home = (object.home !== undefined && object.home !== null)
      ? PortalFrontendConfiguration_FeatureFlags_HomePage.fromPartial(object.home)
      : undefined;
    message.bes = (object.bes !== undefined && object.bes !== null)
      ? PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags.fromPartial(object.bes)
      : undefined;
    message.browser = (object.browser !== undefined && object.browser !== null)
      ? Empty.fromPartial(object.browser)
      : undefined;
    message.scheduler = (object.scheduler !== undefined && object.scheduler !== null)
      ? Empty.fromPartial(object.scheduler)
      : undefined;
    return message;
  },
};

function createBasePortalFrontendConfiguration_FeatureFlags_HomePage(): PortalFrontendConfiguration_FeatureFlags_HomePage {
  return { fileUpload: undefined, instructions: undefined };
}

export const PortalFrontendConfiguration_FeatureFlags_HomePage: MessageFns<
  PortalFrontendConfiguration_FeatureFlags_HomePage
> = {
  encode(
    message: PortalFrontendConfiguration_FeatureFlags_HomePage,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.fileUpload !== undefined) {
      Empty.encode(message.fileUpload, writer.uint32(10).fork()).join();
    }
    if (message.instructions !== undefined) {
      Empty.encode(message.instructions, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortalFrontendConfiguration_FeatureFlags_HomePage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortalFrontendConfiguration_FeatureFlags_HomePage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fileUpload = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instructions = Empty.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortalFrontendConfiguration_FeatureFlags_HomePage {
    return {
      fileUpload: isSet(object.fileUpload)
        ? Empty.fromJSON(object.fileUpload)
        : isSet(object.file_upload)
        ? Empty.fromJSON(object.file_upload)
        : undefined,
      instructions: isSet(object.instructions) ? Empty.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: PortalFrontendConfiguration_FeatureFlags_HomePage): unknown {
    const obj: any = {};
    if (message.fileUpload !== undefined) {
      obj.fileUpload = Empty.toJSON(message.fileUpload);
    }
    if (message.instructions !== undefined) {
      obj.instructions = Empty.toJSON(message.instructions);
    }
    return obj;
  },

  create(
    base?: DeepPartial<PortalFrontendConfiguration_FeatureFlags_HomePage>,
  ): PortalFrontendConfiguration_FeatureFlags_HomePage {
    return PortalFrontendConfiguration_FeatureFlags_HomePage.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<PortalFrontendConfiguration_FeatureFlags_HomePage>,
  ): PortalFrontendConfiguration_FeatureFlags_HomePage {
    const message = createBasePortalFrontendConfiguration_FeatureFlags_HomePage();
    message.fileUpload = (object.fileUpload !== undefined && object.fileUpload !== null)
      ? Empty.fromPartial(object.fileUpload)
      : undefined;
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Empty.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBasePortalFrontendConfiguration_FeatureFlags_BesFeatureFlags(): PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags {
  return {
    pageBuilds: undefined,
    pageInvocations: undefined,
    pageTargets: undefined,
    pageTests: undefined,
    pageTrends: undefined,
  };
}

export const PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags: MessageFns<
  PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags
> = {
  encode(
    message: PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.pageBuilds !== undefined) {
      Empty.encode(message.pageBuilds, writer.uint32(10).fork()).join();
    }
    if (message.pageInvocations !== undefined) {
      Empty.encode(message.pageInvocations, writer.uint32(18).fork()).join();
    }
    if (message.pageTargets !== undefined) {
      Empty.encode(message.pageTargets, writer.uint32(26).fork()).join();
    }
    if (message.pageTests !== undefined) {
      Empty.encode(message.pageTests, writer.uint32(34).fork()).join();
    }
    if (message.pageTrends !== undefined) {
      Empty.encode(message.pageTrends, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortalFrontendConfiguration_FeatureFlags_BesFeatureFlags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pageBuilds = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageInvocations = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pageTargets = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pageTests = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pageTrends = Empty.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags {
    return {
      pageBuilds: isSet(object.pageBuilds)
        ? Empty.fromJSON(object.pageBuilds)
        : isSet(object.page_builds)
        ? Empty.fromJSON(object.page_builds)
        : undefined,
      pageInvocations: isSet(object.pageInvocations)
        ? Empty.fromJSON(object.pageInvocations)
        : isSet(object.page_invocations)
        ? Empty.fromJSON(object.page_invocations)
        : undefined,
      pageTargets: isSet(object.pageTargets)
        ? Empty.fromJSON(object.pageTargets)
        : isSet(object.page_targets)
        ? Empty.fromJSON(object.page_targets)
        : undefined,
      pageTests: isSet(object.pageTests)
        ? Empty.fromJSON(object.pageTests)
        : isSet(object.page_tests)
        ? Empty.fromJSON(object.page_tests)
        : undefined,
      pageTrends: isSet(object.pageTrends)
        ? Empty.fromJSON(object.pageTrends)
        : isSet(object.page_trends)
        ? Empty.fromJSON(object.page_trends)
        : undefined,
    };
  },

  toJSON(message: PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags): unknown {
    const obj: any = {};
    if (message.pageBuilds !== undefined) {
      obj.pageBuilds = Empty.toJSON(message.pageBuilds);
    }
    if (message.pageInvocations !== undefined) {
      obj.pageInvocations = Empty.toJSON(message.pageInvocations);
    }
    if (message.pageTargets !== undefined) {
      obj.pageTargets = Empty.toJSON(message.pageTargets);
    }
    if (message.pageTests !== undefined) {
      obj.pageTests = Empty.toJSON(message.pageTests);
    }
    if (message.pageTrends !== undefined) {
      obj.pageTrends = Empty.toJSON(message.pageTrends);
    }
    return obj;
  },

  create(
    base?: DeepPartial<PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags>,
  ): PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags {
    return PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags>,
  ): PortalFrontendConfiguration_FeatureFlags_BesFeatureFlags {
    const message = createBasePortalFrontendConfiguration_FeatureFlags_BesFeatureFlags();
    message.pageBuilds = (object.pageBuilds !== undefined && object.pageBuilds !== null)
      ? Empty.fromPartial(object.pageBuilds)
      : undefined;
    message.pageInvocations = (object.pageInvocations !== undefined && object.pageInvocations !== null)
      ? Empty.fromPartial(object.pageInvocations)
      : undefined;
    message.pageTargets = (object.pageTargets !== undefined && object.pageTargets !== null)
      ? Empty.fromPartial(object.pageTargets)
      : undefined;
    message.pageTests = (object.pageTests !== undefined && object.pageTests !== null)
      ? Empty.fromPartial(object.pageTests)
      : undefined;
    message.pageTrends = (object.pageTrends !== undefined && object.pageTrends !== null)
      ? Empty.fromPartial(object.pageTrends)
      : undefined;
    return message;
  },
};

function createBasePortalFrontendConfiguration_FooterElement(): PortalFrontendConfiguration_FooterElement {
  return { text: "", href: "", icon: undefined };
}

export const PortalFrontendConfiguration_FooterElement: MessageFns<PortalFrontendConfiguration_FooterElement> = {
  encode(message: PortalFrontendConfiguration_FooterElement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.href !== "") {
      writer.uint32(18).string(message.href);
    }
    if (message.icon !== undefined) {
      PortalFrontendConfiguration_FooterElement_Icon.encode(message.icon, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortalFrontendConfiguration_FooterElement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortalFrontendConfiguration_FooterElement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.href = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.icon = PortalFrontendConfiguration_FooterElement_Icon.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortalFrontendConfiguration_FooterElement {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      href: isSet(object.href) ? globalThis.String(object.href) : "",
      icon: isSet(object.icon) ? PortalFrontendConfiguration_FooterElement_Icon.fromJSON(object.icon) : undefined,
    };
  },

  toJSON(message: PortalFrontendConfiguration_FooterElement): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.href !== "") {
      obj.href = message.href;
    }
    if (message.icon !== undefined) {
      obj.icon = PortalFrontendConfiguration_FooterElement_Icon.toJSON(message.icon);
    }
    return obj;
  },

  create(base?: DeepPartial<PortalFrontendConfiguration_FooterElement>): PortalFrontendConfiguration_FooterElement {
    return PortalFrontendConfiguration_FooterElement.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<PortalFrontendConfiguration_FooterElement>,
  ): PortalFrontendConfiguration_FooterElement {
    const message = createBasePortalFrontendConfiguration_FooterElement();
    message.text = object.text ?? "";
    message.href = object.href ?? "";
    message.icon = (object.icon !== undefined && object.icon !== null)
      ? PortalFrontendConfiguration_FooterElement_Icon.fromPartial(object.icon)
      : undefined;
    return message;
  },
};

function createBasePortalFrontendConfiguration_FooterElement_Icon(): PortalFrontendConfiguration_FooterElement_Icon {
  return { url: undefined, slack: undefined, github: undefined, discord: undefined };
}

export const PortalFrontendConfiguration_FooterElement_Icon: MessageFns<
  PortalFrontendConfiguration_FooterElement_Icon
> = {
  encode(
    message: PortalFrontendConfiguration_FooterElement_Icon,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.url !== undefined) {
      writer.uint32(10).string(message.url);
    }
    if (message.slack !== undefined) {
      Empty.encode(message.slack, writer.uint32(18).fork()).join();
    }
    if (message.github !== undefined) {
      Empty.encode(message.github, writer.uint32(26).fork()).join();
    }
    if (message.discord !== undefined) {
      Empty.encode(message.discord, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortalFrontendConfiguration_FooterElement_Icon {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortalFrontendConfiguration_FooterElement_Icon();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.slack = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.github = Empty.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.discord = Empty.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortalFrontendConfiguration_FooterElement_Icon {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : undefined,
      slack: isSet(object.slack) ? Empty.fromJSON(object.slack) : undefined,
      github: isSet(object.github) ? Empty.fromJSON(object.github) : undefined,
      discord: isSet(object.discord) ? Empty.fromJSON(object.discord) : undefined,
    };
  },

  toJSON(message: PortalFrontendConfiguration_FooterElement_Icon): unknown {
    const obj: any = {};
    if (message.url !== undefined) {
      obj.url = message.url;
    }
    if (message.slack !== undefined) {
      obj.slack = Empty.toJSON(message.slack);
    }
    if (message.github !== undefined) {
      obj.github = Empty.toJSON(message.github);
    }
    if (message.discord !== undefined) {
      obj.discord = Empty.toJSON(message.discord);
    }
    return obj;
  },

  create(
    base?: DeepPartial<PortalFrontendConfiguration_FooterElement_Icon>,
  ): PortalFrontendConfiguration_FooterElement_Icon {
    return PortalFrontendConfiguration_FooterElement_Icon.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<PortalFrontendConfiguration_FooterElement_Icon>,
  ): PortalFrontendConfiguration_FooterElement_Icon {
    const message = createBasePortalFrontendConfiguration_FooterElement_Icon();
    message.url = object.url ?? undefined;
    message.slack = (object.slack !== undefined && object.slack !== null) ? Empty.fromPartial(object.slack) : undefined;
    message.github = (object.github !== undefined && object.github !== null)
      ? Empty.fromPartial(object.github)
      : undefined;
    message.discord = (object.discord !== undefined && object.discord !== null)
      ? Empty.fromPartial(object.discord)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
