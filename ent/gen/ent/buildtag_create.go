// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/buildbarn/bb-portal/ent/gen/ent/build"
	"github.com/buildbarn/bb-portal/ent/gen/ent/buildtag"
)

// BuildTagCreate is the builder for creating a BuildTag entity.
type BuildTagCreate struct {
	config
	mutation *BuildTagMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetBuildID sets the "build_id" field.
func (btc *BuildTagCreate) SetBuildID(i int64) *BuildTagCreate {
	btc.mutation.SetBuildID(i)
	return btc
}

// SetKey sets the "key" field.
func (btc *BuildTagCreate) SetKey(s string) *BuildTagCreate {
	btc.mutation.SetKey(s)
	return btc
}

// SetValue sets the "value" field.
func (btc *BuildTagCreate) SetValue(s string) *BuildTagCreate {
	btc.mutation.SetValue(s)
	return btc
}

// SetID sets the "id" field.
func (btc *BuildTagCreate) SetID(i int64) *BuildTagCreate {
	btc.mutation.SetID(i)
	return btc
}

// SetBuild sets the "build" edge to the Build entity.
func (btc *BuildTagCreate) SetBuild(b *Build) *BuildTagCreate {
	return btc.SetBuildID(b.ID)
}

// Mutation returns the BuildTagMutation object of the builder.
func (btc *BuildTagCreate) Mutation() *BuildTagMutation {
	return btc.mutation
}

// Save creates the BuildTag in the database.
func (btc *BuildTagCreate) Save(ctx context.Context) (*BuildTag, error) {
	return withHooks(ctx, btc.sqlSave, btc.mutation, btc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (btc *BuildTagCreate) SaveX(ctx context.Context) *BuildTag {
	v, err := btc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (btc *BuildTagCreate) Exec(ctx context.Context) error {
	_, err := btc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (btc *BuildTagCreate) ExecX(ctx context.Context) {
	if err := btc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (btc *BuildTagCreate) check() error {
	if _, ok := btc.mutation.BuildID(); !ok {
		return &ValidationError{Name: "build_id", err: errors.New(`ent: missing required field "BuildTag.build_id"`)}
	}
	if _, ok := btc.mutation.Key(); !ok {
		return &ValidationError{Name: "key", err: errors.New(`ent: missing required field "BuildTag.key"`)}
	}
	if _, ok := btc.mutation.Value(); !ok {
		return &ValidationError{Name: "value", err: errors.New(`ent: missing required field "BuildTag.value"`)}
	}
	if len(btc.mutation.BuildIDs()) == 0 {
		return &ValidationError{Name: "build", err: errors.New(`ent: missing required edge "BuildTag.build"`)}
	}
	return nil
}

func (btc *BuildTagCreate) sqlSave(ctx context.Context) (*BuildTag, error) {
	if err := btc.check(); err != nil {
		return nil, err
	}
	_node, _spec := btc.createSpec()
	if err := sqlgraph.CreateNode(ctx, btc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int64(id)
	}
	btc.mutation.id = &_node.ID
	btc.mutation.done = true
	return _node, nil
}

func (btc *BuildTagCreate) createSpec() (*BuildTag, *sqlgraph.CreateSpec) {
	var (
		_node = &BuildTag{config: btc.config}
		_spec = sqlgraph.NewCreateSpec(buildtag.Table, sqlgraph.NewFieldSpec(buildtag.FieldID, field.TypeInt64))
	)
	_spec.OnConflict = btc.conflict
	if id, ok := btc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := btc.mutation.Key(); ok {
		_spec.SetField(buildtag.FieldKey, field.TypeString, value)
		_node.Key = value
	}
	if value, ok := btc.mutation.Value(); ok {
		_spec.SetField(buildtag.FieldValue, field.TypeString, value)
		_node.Value = value
	}
	if nodes := btc.mutation.BuildIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   buildtag.BuildTable,
			Columns: []string{buildtag.BuildColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(build.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.BuildID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.BuildTag.Create().
//		SetBuildID(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.BuildTagUpsert) {
//			SetBuildID(v+v).
//		}).
//		Exec(ctx)
func (btc *BuildTagCreate) OnConflict(opts ...sql.ConflictOption) *BuildTagUpsertOne {
	btc.conflict = opts
	return &BuildTagUpsertOne{
		create: btc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.BuildTag.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (btc *BuildTagCreate) OnConflictColumns(columns ...string) *BuildTagUpsertOne {
	btc.conflict = append(btc.conflict, sql.ConflictColumns(columns...))
	return &BuildTagUpsertOne{
		create: btc,
	}
}

type (
	// BuildTagUpsertOne is the builder for "upsert"-ing
	//  one BuildTag node.
	BuildTagUpsertOne struct {
		create *BuildTagCreate
	}

	// BuildTagUpsert is the "OnConflict" setter.
	BuildTagUpsert struct {
		*sql.UpdateSet
	}
)

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.BuildTag.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(buildtag.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *BuildTagUpsertOne) UpdateNewValues() *BuildTagUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(buildtag.FieldID)
		}
		if _, exists := u.create.mutation.BuildID(); exists {
			s.SetIgnore(buildtag.FieldBuildID)
		}
		if _, exists := u.create.mutation.Key(); exists {
			s.SetIgnore(buildtag.FieldKey)
		}
		if _, exists := u.create.mutation.Value(); exists {
			s.SetIgnore(buildtag.FieldValue)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.BuildTag.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *BuildTagUpsertOne) Ignore() *BuildTagUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *BuildTagUpsertOne) DoNothing() *BuildTagUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the BuildTagCreate.OnConflict
// documentation for more info.
func (u *BuildTagUpsertOne) Update(set func(*BuildTagUpsert)) *BuildTagUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&BuildTagUpsert{UpdateSet: update})
	}))
	return u
}

// Exec executes the query.
func (u *BuildTagUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for BuildTagCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *BuildTagUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *BuildTagUpsertOne) ID(ctx context.Context) (id int64, err error) {
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *BuildTagUpsertOne) IDX(ctx context.Context) int64 {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// BuildTagCreateBulk is the builder for creating many BuildTag entities in bulk.
type BuildTagCreateBulk struct {
	config
	err      error
	builders []*BuildTagCreate
	conflict []sql.ConflictOption
}

// Save creates the BuildTag entities in the database.
func (btcb *BuildTagCreateBulk) Save(ctx context.Context) ([]*BuildTag, error) {
	if btcb.err != nil {
		return nil, btcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(btcb.builders))
	nodes := make([]*BuildTag, len(btcb.builders))
	mutators := make([]Mutator, len(btcb.builders))
	for i := range btcb.builders {
		func(i int, root context.Context) {
			builder := btcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*BuildTagMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, btcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = btcb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, btcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, btcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (btcb *BuildTagCreateBulk) SaveX(ctx context.Context) []*BuildTag {
	v, err := btcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (btcb *BuildTagCreateBulk) Exec(ctx context.Context) error {
	_, err := btcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (btcb *BuildTagCreateBulk) ExecX(ctx context.Context) {
	if err := btcb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.BuildTag.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.BuildTagUpsert) {
//			SetBuildID(v+v).
//		}).
//		Exec(ctx)
func (btcb *BuildTagCreateBulk) OnConflict(opts ...sql.ConflictOption) *BuildTagUpsertBulk {
	btcb.conflict = opts
	return &BuildTagUpsertBulk{
		create: btcb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.BuildTag.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (btcb *BuildTagCreateBulk) OnConflictColumns(columns ...string) *BuildTagUpsertBulk {
	btcb.conflict = append(btcb.conflict, sql.ConflictColumns(columns...))
	return &BuildTagUpsertBulk{
		create: btcb,
	}
}

// BuildTagUpsertBulk is the builder for "upsert"-ing
// a bulk of BuildTag nodes.
type BuildTagUpsertBulk struct {
	create *BuildTagCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.BuildTag.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(buildtag.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *BuildTagUpsertBulk) UpdateNewValues() *BuildTagUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(buildtag.FieldID)
			}
			if _, exists := b.mutation.BuildID(); exists {
				s.SetIgnore(buildtag.FieldBuildID)
			}
			if _, exists := b.mutation.Key(); exists {
				s.SetIgnore(buildtag.FieldKey)
			}
			if _, exists := b.mutation.Value(); exists {
				s.SetIgnore(buildtag.FieldValue)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.BuildTag.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *BuildTagUpsertBulk) Ignore() *BuildTagUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *BuildTagUpsertBulk) DoNothing() *BuildTagUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the BuildTagCreateBulk.OnConflict
// documentation for more info.
func (u *BuildTagUpsertBulk) Update(set func(*BuildTagUpsert)) *BuildTagUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&BuildTagUpsert{UpdateSet: update})
	}))
	return u
}

// Exec executes the query.
func (u *BuildTagUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the BuildTagCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for BuildTagCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *BuildTagUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
