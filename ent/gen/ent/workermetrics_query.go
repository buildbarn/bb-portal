// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/buildbarn/bb-portal/ent/gen/ent/metrics"
	"github.com/buildbarn/bb-portal/ent/gen/ent/predicate"
	"github.com/buildbarn/bb-portal/ent/gen/ent/workerid"
	"github.com/buildbarn/bb-portal/ent/gen/ent/workermetrics"
	"github.com/buildbarn/bb-portal/ent/gen/ent/workerstats"
)

// WorkerMetricsQuery is the builder for querying WorkerMetrics entities.
type WorkerMetricsQuery struct {
	config
	ctx                  *QueryContext
	order                []workermetrics.OrderOption
	inters               []Interceptor
	predicates           []predicate.WorkerMetrics
	withMetrics          *MetricsQuery
	withWorkerIds        *WorkerIDQuery
	withWorkerStats      *WorkerStatsQuery
	withFKs              bool
	modifiers            []func(*sql.Selector)
	loadTotal            []func(context.Context, []*WorkerMetrics) error
	withNamedWorkerIds   map[string]*WorkerIDQuery
	withNamedWorkerStats map[string]*WorkerStatsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkerMetricsQuery builder.
func (_q *WorkerMetricsQuery) Where(ps ...predicate.WorkerMetrics) *WorkerMetricsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *WorkerMetricsQuery) Limit(limit int) *WorkerMetricsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *WorkerMetricsQuery) Offset(offset int) *WorkerMetricsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *WorkerMetricsQuery) Unique(unique bool) *WorkerMetricsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *WorkerMetricsQuery) Order(o ...workermetrics.OrderOption) *WorkerMetricsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMetrics chains the current query on the "metrics" edge.
func (_q *WorkerMetricsQuery) QueryMetrics() *MetricsQuery {
	query := (&MetricsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workermetrics.Table, workermetrics.FieldID, selector),
			sqlgraph.To(metrics.Table, metrics.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workermetrics.MetricsTable, workermetrics.MetricsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWorkerIds chains the current query on the "worker_ids" edge.
func (_q *WorkerMetricsQuery) QueryWorkerIds() *WorkerIDQuery {
	query := (&WorkerIDClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workermetrics.Table, workermetrics.FieldID, selector),
			sqlgraph.To(workerid.Table, workerid.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workermetrics.WorkerIdsTable, workermetrics.WorkerIdsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWorkerStats chains the current query on the "worker_stats" edge.
func (_q *WorkerMetricsQuery) QueryWorkerStats() *WorkerStatsQuery {
	query := (&WorkerStatsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workermetrics.Table, workermetrics.FieldID, selector),
			sqlgraph.To(workerstats.Table, workerstats.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workermetrics.WorkerStatsTable, workermetrics.WorkerStatsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkerMetrics entity from the query.
// Returns a *NotFoundError when no WorkerMetrics was found.
func (_q *WorkerMetricsQuery) First(ctx context.Context) (*WorkerMetrics, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workermetrics.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *WorkerMetricsQuery) FirstX(ctx context.Context) *WorkerMetrics {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkerMetrics ID from the query.
// Returns a *NotFoundError when no WorkerMetrics ID was found.
func (_q *WorkerMetricsQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workermetrics.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *WorkerMetricsQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkerMetrics entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkerMetrics entity is found.
// Returns a *NotFoundError when no WorkerMetrics entities are found.
func (_q *WorkerMetricsQuery) Only(ctx context.Context) (*WorkerMetrics, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workermetrics.Label}
	default:
		return nil, &NotSingularError{workermetrics.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *WorkerMetricsQuery) OnlyX(ctx context.Context) *WorkerMetrics {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkerMetrics ID in the query.
// Returns a *NotSingularError when more than one WorkerMetrics ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *WorkerMetricsQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workermetrics.Label}
	default:
		err = &NotSingularError{workermetrics.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *WorkerMetricsQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkerMetricsSlice.
func (_q *WorkerMetricsQuery) All(ctx context.Context) ([]*WorkerMetrics, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkerMetrics, *WorkerMetricsQuery]()
	return withInterceptors[[]*WorkerMetrics](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *WorkerMetricsQuery) AllX(ctx context.Context) []*WorkerMetrics {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkerMetrics IDs.
func (_q *WorkerMetricsQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(workermetrics.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *WorkerMetricsQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *WorkerMetricsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*WorkerMetricsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *WorkerMetricsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *WorkerMetricsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *WorkerMetricsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkerMetricsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *WorkerMetricsQuery) Clone() *WorkerMetricsQuery {
	if _q == nil {
		return nil
	}
	return &WorkerMetricsQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]workermetrics.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.WorkerMetrics{}, _q.predicates...),
		withMetrics:     _q.withMetrics.Clone(),
		withWorkerIds:   _q.withWorkerIds.Clone(),
		withWorkerStats: _q.withWorkerStats.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMetrics tells the query-builder to eager-load the nodes that are connected to
// the "metrics" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkerMetricsQuery) WithMetrics(opts ...func(*MetricsQuery)) *WorkerMetricsQuery {
	query := (&MetricsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMetrics = query
	return _q
}

// WithWorkerIds tells the query-builder to eager-load the nodes that are connected to
// the "worker_ids" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkerMetricsQuery) WithWorkerIds(opts ...func(*WorkerIDQuery)) *WorkerMetricsQuery {
	query := (&WorkerIDClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWorkerIds = query
	return _q
}

// WithWorkerStats tells the query-builder to eager-load the nodes that are connected to
// the "worker_stats" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkerMetricsQuery) WithWorkerStats(opts ...func(*WorkerStatsQuery)) *WorkerMetricsQuery {
	query := (&WorkerStatsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWorkerStats = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProcessID uint32 `json:"process_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkerMetrics.Query().
//		GroupBy(workermetrics.FieldProcessID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *WorkerMetricsQuery) GroupBy(field string, fields ...string) *WorkerMetricsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkerMetricsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = workermetrics.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProcessID uint32 `json:"process_id,omitempty"`
//	}
//
//	client.WorkerMetrics.Query().
//		Select(workermetrics.FieldProcessID).
//		Scan(ctx, &v)
func (_q *WorkerMetricsQuery) Select(fields ...string) *WorkerMetricsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &WorkerMetricsSelect{WorkerMetricsQuery: _q}
	sbuild.label = workermetrics.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkerMetricsSelect configured with the given aggregations.
func (_q *WorkerMetricsQuery) Aggregate(fns ...AggregateFunc) *WorkerMetricsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *WorkerMetricsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !workermetrics.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *WorkerMetricsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkerMetrics, error) {
	var (
		nodes       = []*WorkerMetrics{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withMetrics != nil,
			_q.withWorkerIds != nil,
			_q.withWorkerStats != nil,
		}
	)
	if _q.withMetrics != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, workermetrics.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkerMetrics).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkerMetrics{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMetrics; query != nil {
		if err := _q.loadMetrics(ctx, query, nodes, nil,
			func(n *WorkerMetrics, e *Metrics) { n.Edges.Metrics = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withWorkerIds; query != nil {
		if err := _q.loadWorkerIds(ctx, query, nodes,
			func(n *WorkerMetrics) { n.Edges.WorkerIds = []*WorkerID{} },
			func(n *WorkerMetrics, e *WorkerID) { n.Edges.WorkerIds = append(n.Edges.WorkerIds, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withWorkerStats; query != nil {
		if err := _q.loadWorkerStats(ctx, query, nodes,
			func(n *WorkerMetrics) { n.Edges.WorkerStats = []*WorkerStats{} },
			func(n *WorkerMetrics, e *WorkerStats) { n.Edges.WorkerStats = append(n.Edges.WorkerStats, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedWorkerIds {
		if err := _q.loadWorkerIds(ctx, query, nodes,
			func(n *WorkerMetrics) { n.appendNamedWorkerIds(name) },
			func(n *WorkerMetrics, e *WorkerID) { n.appendNamedWorkerIds(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedWorkerStats {
		if err := _q.loadWorkerStats(ctx, query, nodes,
			func(n *WorkerMetrics) { n.appendNamedWorkerStats(name) },
			func(n *WorkerMetrics, e *WorkerStats) { n.appendNamedWorkerStats(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *WorkerMetricsQuery) loadMetrics(ctx context.Context, query *MetricsQuery, nodes []*WorkerMetrics, init func(*WorkerMetrics), assign func(*WorkerMetrics, *Metrics)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*WorkerMetrics)
	for i := range nodes {
		if nodes[i].metrics_worker_metrics == nil {
			continue
		}
		fk := *nodes[i].metrics_worker_metrics
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(metrics.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "metrics_worker_metrics" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *WorkerMetricsQuery) loadWorkerIds(ctx context.Context, query *WorkerIDQuery, nodes []*WorkerMetrics, init func(*WorkerMetrics), assign func(*WorkerMetrics, *WorkerID)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*WorkerMetrics)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.WorkerID(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workermetrics.WorkerIdsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.worker_metrics_worker_ids
		if fk == nil {
			return fmt.Errorf(`foreign-key "worker_metrics_worker_ids" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "worker_metrics_worker_ids" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *WorkerMetricsQuery) loadWorkerStats(ctx context.Context, query *WorkerStatsQuery, nodes []*WorkerMetrics, init func(*WorkerMetrics), assign func(*WorkerMetrics, *WorkerStats)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*WorkerMetrics)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.WorkerStats(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workermetrics.WorkerStatsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.worker_metrics_worker_stats
		if fk == nil {
			return fmt.Errorf(`foreign-key "worker_metrics_worker_stats" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "worker_metrics_worker_stats" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *WorkerMetricsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *WorkerMetricsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workermetrics.Table, workermetrics.Columns, sqlgraph.NewFieldSpec(workermetrics.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workermetrics.FieldID)
		for i := range fields {
			if fields[i] != workermetrics.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *WorkerMetricsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(workermetrics.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = workermetrics.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedWorkerIds tells the query-builder to eager-load the nodes that are connected to the "worker_ids"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkerMetricsQuery) WithNamedWorkerIds(name string, opts ...func(*WorkerIDQuery)) *WorkerMetricsQuery {
	query := (&WorkerIDClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedWorkerIds == nil {
		_q.withNamedWorkerIds = make(map[string]*WorkerIDQuery)
	}
	_q.withNamedWorkerIds[name] = query
	return _q
}

// WithNamedWorkerStats tells the query-builder to eager-load the nodes that are connected to the "worker_stats"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkerMetricsQuery) WithNamedWorkerStats(name string, opts ...func(*WorkerStatsQuery)) *WorkerMetricsQuery {
	query := (&WorkerStatsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedWorkerStats == nil {
		_q.withNamedWorkerStats = make(map[string]*WorkerStatsQuery)
	}
	_q.withNamedWorkerStats[name] = query
	return _q
}

// WorkerMetricsGroupBy is the group-by builder for WorkerMetrics entities.
type WorkerMetricsGroupBy struct {
	selector
	build *WorkerMetricsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *WorkerMetricsGroupBy) Aggregate(fns ...AggregateFunc) *WorkerMetricsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *WorkerMetricsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkerMetricsQuery, *WorkerMetricsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *WorkerMetricsGroupBy) sqlScan(ctx context.Context, root *WorkerMetricsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkerMetricsSelect is the builder for selecting fields of WorkerMetrics entities.
type WorkerMetricsSelect struct {
	*WorkerMetricsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *WorkerMetricsSelect) Aggregate(fns ...AggregateFunc) *WorkerMetricsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *WorkerMetricsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkerMetricsQuery, *WorkerMetricsSelect](ctx, _s.WorkerMetricsQuery, _s, _s.inters, v)
}

func (_s *WorkerMetricsSelect) sqlScan(ctx context.Context, root *WorkerMetricsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
